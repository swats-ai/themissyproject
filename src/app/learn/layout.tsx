import type { ReactNode } from "react";

export const metadata = { title: "Learn | The Missy Project" };

export default function LearnLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
